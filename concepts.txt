// Access the pod

<pod-ip>.<namespace>.pod.cluster.local


// through deployment and service

<service-name>.<namespace>.svc.cluster.local


// through statefulset and service

<pod-name>.<service-name>.<namespace>.svc.cluster.local

here pod names will be <statefulsetName>-0 , <statefulsetName>-1 etc....





kubectl port-forward pod/my-pod 8080:80

Access http://localhost:8080 → traffic goes to my-pod:80.




kubectl port-forward service/my-service 9090:9090

Access http://localhost:9090 → traffic goes to the Service’s backend Pods.



// Normally, in a StatefulSet you don’t manually create PVCs — they are generated automatically from the volumeClaimTemplates.



